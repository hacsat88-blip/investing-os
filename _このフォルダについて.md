# investingOS 運用フォルダ（唯一の正本）

2026-09-22更新。**ここが唯一の正本フォルダです。** 運用も改修も、このフォルダだけで行います。
以前あった改修専用フォルダ `investing_OS_data` と空の `investing_os` は役目を終えました。新しい更新先にしないでください。

## 2つの正本を混同しない
| 対象 | 正本 | 戻し方 |
|---|---|---|
| コード・ナレッジ・設定 | git（`main` ブランチ） | `git restore` / `git revert` |
| CSV台帳 | `data/current.json` が指す `data/revisions/<version>/` | アプリ画面の過去版復元 |

`data/` `backups/` `monitoring/` `proposals/` はgit管理外です（`.gitignore`）。

## 中身
| 場所 | 役割 |
|---|---|
| `app/` | アプリ本体。`起動.command` で起動 |
| `knowledge/` | 業務手順。経路の正本は `40-data-sources.md`、フォルダ運用は `80-repo-operations.md` |
| `data/` | **CSV台帳の正本。** 改修作業では触らない |
| `backups/` | 保存ごとのZIP。`python3 app/store.py backup` で追加 |
| `monitoring/` | 監視の実行記録と状態 |
| `proposals/` | 台帳への更新案（PROPOSED）。画面で承認して初めて反映 |
| `examples/` | `fnd/` の入出力例 |
| `dev/tests/` | `test_upgrades.py` 32件、`test_screening.py` 16件。いずれも一時フォルダを使い、実台帳を書き換えない |
| `dev/audit/` | QA記録、反映履歴（`DEPLOYMENT-STATUS.md`）、反映前スナップショット |
| `dev/work/` | v4.3→v4.4の移行スクリプト |
| `dev/archive/` | 退避物。`data-snapshot-2026-09-21/` は旧改修フォルダにあった2026-09-21時点のCSVの写し（**更新しない**） |
| `_setup/` | `investingOS_ChatGPT` 入口の差し替えファイル |

## 改修の手順
`knowledge/80-repo-operations.md` の §3 に従ってください。要点は、アプリを終了 → `app/store.py backup` → `git switch -c fix/<内容>` → 改修 → テスト全件OK → `main` へマージ → `dev/audit/DEPLOYMENT-STATUS.md` に記録、です。
