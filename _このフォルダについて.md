# investingOS v4.4 — 運用フォルダ（正本）

作成 2026-09-21。**このフォルダが運用の正本です。** 保有・判断・相場の更新はここだけで行います。

## 起動
`起動.command` をダブルクリック。相対パスで動くので、フォルダごと移動しても動きます。

## 中身
| フォルダ | 役割 |
|---|---|
| `app/` | アプリ本体（Python標準ライブラリのみ。外部通信なし） |
| `data/` | **CSV正本**。`current.json` が指す `revisions/<版>/` の12種CSV |
| `backups/` | 保存のたびに自動作成される版ZIP |
| `knowledge/` | 分析・運用の手順書12文書 |
| `monitoring/` | 監視の実行記録 |
| `proposals/` | AIが作った更新案JSONの受け渡し場所 |
| `examples/` | `fnd/` スクリーニングの入力例 |

## 正本の原則
- 正本はこのフォルダの `data/` のCSVのみ。AIの会話・画面・Artifact・Projectは正本にしない
- AIは提案まで。保存はアプリ画面でのあなたの承認
- 売買の自動発注は行わない

## 以前のフォルダの扱い（2026-09-21以降は履歴）
- `~/Desktop/investing_os` — 移行元。以後は更新しない
- `~/Documents/Codex/2026-09-13/investingos-google-csv-csv-ai-artifact` — v4.3スナップショット
- `~/Desktop/investingOS_ChatGPT` — 旧AI入口。**2026-09-21に削除済み**。中身の控えは investing_OS_data/dev/archive/ と investing_os/_archive/ に残っています
- `Documents/Codex/.../outputs/investingOS_ChatGPT-update` — 旧入口の配布パッケージ。2026-09-21に整理し、運用フォルダを指すよう修正済み

## 改修用フォルダ
`~/Desktop/investing_OS_data` にテスト・監査履歴・移行スクリプトを置いています。
**改修はあちらで行い、検証が済んだファイルだけをこちらへ反映**してください。
あちらの `data` 系は複製時点のスナップショットであり、正本ではありません。
